# Pace

A minimal React and TypeScript pace converter built with Vite.

Convert between `min/mile` and `min/km` paces without extra UI dependencies.

## Scripts

```sh
pnpm run dev
pnpm run build
pnpm run preview
```
